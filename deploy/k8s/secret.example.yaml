# Example Secret only. Replace CHANGE_ME before pointing this at a real database.
# Do not commit real passwords. Copy to deploy/k8s/secret.yaml (gitignored) if
# you need a local override, and keep this file as the placeholder.
apiVersion: v1
kind: Secret
metadata:
  name: cement-copilot-db
  labels:
    app: cement-copilot
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg2://copilot:CHANGE_ME@postgres.example.invalid:5432/cement_copilot"
