apiVersion: v1
kind: Service
metadata:
  name: cement-copilot-dashboard
  labels:
    app: cement-copilot-dashboard
spec:
  selector:
    app: cement-copilot-dashboard
  ports:
    - name: http
      port: 8501
      targetPort: http
