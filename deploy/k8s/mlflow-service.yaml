apiVersion: v1
kind: Service
metadata:
  name: cement-copilot-mlflow
  labels:
    app: cement-copilot-mlflow
spec:
  selector:
    app: cement-copilot-mlflow
  ports:
    - name: http
      port: 5000
      targetPort: http
